express → لإنشاء خادم الويب

mysql → للتعامل مع قاعدة البيانات

dotenv → لتحميل المتغيرات من .env

bcryptjs → لتشفير كلمات المرور

jsonwebtoken → لإنشاء التوكن JWT

cookie-parser → للتعامل مع الكوكيز

express-session → لإدارة الجلسات

cors → للسماح بالتعامل مع API من دومينات أخرى

============================================

express → لإنشاء الـ API.

bcryptjs → لتشفير كلمات المرور.

body-parser → لتحليل البيانات القادمة من الـ request.